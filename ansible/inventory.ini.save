Write-Host "`n📍 You're in WSL - Ready to use Ansible!`n" -ForegroundColor Green; Write-Host "Quick Commands:`n" -ForegroundColor Cyan; Write-Host '1. Navigate to ansible folder:' -ForegroundColor Yellow; Write-Host '   cd "/mnt/c/Users/Shanu Pudi/Documents/3-1/Rhythm/ansible"`n' -ForegroundColor White; Write-Host '2. View inventory (server config):' -ForegroundColor Yellow; Write-Host '   cat inventory.ini`n' -ForegroundColor White; Write-Host '3. Edit inventory to add your server:' -ForegroundColor Yellow; Write-Host '   nano inventory.ini`n' -ForegroundColor White; Write-Host '4. Test connection:' -ForegroundColor Yellow; Write-Host '   ansible appservers -i inventory.ini -m ping`n' -ForegroundColor White; Write-Host '5. Deploy:' -ForegroundColor Yellow; Write-Host '   ansible-playbook -i inventory.ini deploy-docker-compose.yml`n' -ForegroundColor White; Write-Host "💡 Tip: If you don't have a remote server, you can test locally!" -ForegroundColor Magenta; Write-Host "   Edit inventory.ini and uncomment 'localhost ansible_connection=local'`n" -ForegroundColor White
[appservers]
# Example configurations - uncomment and modify as needed

# Single server with SSH key (recommended)
# 192.168.1.100 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

# Multiple servers
# server1.example.com ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/id_rsa
# server2.example.com ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/id_rsa

# Server with custom SSH port
# 192.168.1.100 ansible_user=ubuntu ansible_port=2222 ansible_ssh_private_key_file=~/.ssh/id_rsa

# AWS EC2 instance
# ec2-xx-xxx-xxx-xxx.compute-1.amazonaws.com ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/aws-key.pem

# Local testing (uncomment to use)
# localhost ansible_connection=local

[appservers:vars]
ansible_python_interpreter=/usr/bin/python3
